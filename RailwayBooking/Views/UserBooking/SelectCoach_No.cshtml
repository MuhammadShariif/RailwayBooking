@model RailwayBooking.Models.UserDashboard

@{
    ViewBag.Title = "SelectCoach_No";
}

<h2>SelectCoach_No</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>UserDashboard</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.Label("Coach")
    <datalist id="CoachList">
        @foreach (var item in (List<int?>)TempData["Coach_No"])
        {
            <option>@item</option>
        }
    </datalist>
    <input type="text" name="selected_coach_no" required="" list="CoachList" />
    <br />
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
