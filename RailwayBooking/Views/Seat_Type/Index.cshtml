@model IEnumerable<RailwayBooking.Seat_Type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Seat_Type_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Seat_Type_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Seat_Type_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Seat_Type_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Seat_Type_ID })
        </td>
    </tr>
}

</table>
